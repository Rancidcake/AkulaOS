# AkulaOS live ISO — mkinitcpio hooks for archiso live boot.
# The 'archiso' hook finds and mounts the squashfs root filesystem from the USB/ISO.
# Without this, the kernel boots but can't find the live root and enters emergency mode.

HOOKS=(base udev memdisk archiso block filesystems)
